import { describe, expect, it } from 'vitest';

// Placeholder da Fase 0: prova que o runner roda. Substituído pelos testes das regras puras na Fase 1.
describe('smoke', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
